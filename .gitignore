# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Env
.env

# Demo-only product photos — real brand product photography, downloaded for
# local demo use only. Never committed/pushed (licensing). See
# src/data/productImages.ts, which maps product_id -> these local paths and
# is committed (just filenames, no image bytes) so the app degrades
# gracefully to its placeholder icon on any clone without this folder.
public/product-images/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
